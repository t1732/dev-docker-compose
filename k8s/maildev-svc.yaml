apiVersion: v1
kind: Service
metadata:
  name: maildev
  labels:
    env: development
    tier: web
    app: maildev
spec:
  ports:
    - name: "http"
      port: 8025
      targetPort: 80
    - name: "smtp"
      port: 1025
      targetPort: 25
  selector:
    env: development
    tier: web
    app: maildev
  type: LoadBalancer
